# Unit 9 · Gradient Descent — widget markup + JS spec

You are writing TWO files:
1. `/home/claude/mfml-site/dev/tpl/u9-widgets.html` — the markup of nine widgets, each preceded by a marker line `<!--@W1-->` … `<!--@W9-->` exactly like `tpl/u8-widgets.html` (read that file first and copy its structure: `.widget.reveal` → `.widget-head` (`.w-title`, `.w-sub`) → `p.try` → optional `.tabs`/`.presets` → `.wrow` with a wide `.wcol` (flex:2.3 1 400px) holding the SVG (`class="svg-lab"`, `role="img"`, `aria-label`) or a `div.stage3d` (+ `<p class="stage-tap">drag the picture to orbit</p>`) and a narrow `.wcol` (flex:1 1 250px) holding `.controls` (`.ctl` rows: `<label for>` + `<input type=range>` + `<output id="ID-o">`), `.presets` (`.play` / `.preset` buttons), `.readout`, `.verdict`, `p.note`). Every "Try:" line must be one concrete instruction and one thing to notice. KaTeX `\(…\)` is allowed in `.try`/`.note`. Never write MFML/ZC416/BITS/WILP.
2. `/home/claude/mfml-site/dev/tpl/u9-widgets.js` — pasted verbatim inside the page's main IIFE *after* `tpl/u9-shared.js` (identical to `tpl/u8-shared.js` except the storage key — read it: it defines every helper you may use: `store, el, txt, CIN, isLight, glo, G, mountStage, remount, hud, hint, tube, slot, sparkTex, plane, arrow, fmt, planePt, edge, T3, picker, grab, aimTube, overlay, lab, fadeAttr, ribbon, contourSegs, liftedContours, glowDot, bindCtl, tabs, tween, RM, easeIO, clamp01`). Do NOT redefine any of them. `"use strict"` is in force; every widget is its own `(function(){ ... })();` block guarded by `if(!box) return;`. The hero container is `<div id="hero-3d">` (a `.hero-stage` opener identical in structure to Unit 8's — its markup is NOT yours; only wire it).

Reference implementations to copy patterns from (they are the house style — read them before writing a line):
- `tpl/u8-widgets.js`: everything — the hero opening shot (`mountStage` + `remount` on the 760px crossing, starfield, `ctx.hero.apply`), W4 `w-walk` (3D `prim.surface` + `ribbon` + `tube` + `slot` labels + `hud` + `grab(handle,()=>false,()=>{})`), W5 `w-judge` (live surface rewrite, `liftedContours`, HUD probe in `update`), W1/W3 (SVG stages with `plane`, `glo`, `txt`, tweened ▶ stories), W8 (bar chart on a log scale).
- `tpl/cinema.js`: the `Cinema` API (`stage3d`, `prim.surface/grid/arrow/dot/path/glass/label`, `hex`, `colors`, `ramp`, `ease`, `reduced`, `glowFilter`).

House rules (hard):
- Colours always from the live palette: `CIN.colors()` → `hx('s1')` etc. for three.js, `var(--s1)` strings for SVG. Never hardcode hex. Light theme: glow off (`glo()` returns null; `isLight`), emissive lower — the helpers handle it. Semantic colours: s1 = the function/surface, s2 = uphill/gradient/"bad", s3 = descent/"good", s4 = the current point/highlight, s7 = second series, `var(--critical)` for errors/overshoot.
- Every 3D stage: `mountStage(box, build)` where `build()` returns the `CIN.stage3d(...)` handle; `fill:true` (container has CSS height via `.stage3d`); `autoRotate` ≈ .1, `autoRotateStopsOnUser:true`; add `hud(box,...)` + `hint(box,'drag to orbit')`; call `handle.requestRender()` after any state change; keep scene objects in a closure `sc` reset to null at the start of `build()` (theme flips rebuild every stage via `remount`). `res ≤ 80` for surfaces. Coordinates: math (x, y, z-up) → three.js via `P=(x,y,z)=>[x, z*ZS, -y]`.
- SVG stages: `svg.innerHTML=''` then rebuild; `const glow=glo(svg)`; key strokes `filter:glow||'none'`; fonts via `txt(...)`; readable ≥ 10px at viewBox scale; nothing outside the viewBox.
- Motion only through `tween(dur,step,done)` (respects reduced motion) or a `stage3d` `update(ctx,t,dt)` that returns `false` under `CIN.reduced`. `▶` buttons run a 2–4 s story; a second press restarts it (stop the previous tween chain: keep a `run` token and ignore stale frames).
- `bindCtl(id, fn, fmtr)` wires a range + its `<output id="id-o">`; call the returned updater once to initialise. `tabs(bar, t=>{...})` wires a `.tabs` bar. Presets: set `aria-pressed` on the clicked one, clear the others.
- Readouts (`.readout`) are `innerHTML` with `<b>` numbers; verdicts set `verd.className='verdict good|bad|info'` + `verd.textContent`.
- No console errors, ever; every control must fire without throwing at extreme slider values (guard NaN/∞; clamp diverging iterates: stop a run when |x| > 1e6 and say so).
- `fmt(v,0)` is broken (returns '' for 0) — use `Math.round` or `toFixed(0)` for integers. Use `.toFixed` where a fixed width matters; `toExponential(2)` for errors < 1e-3.

---------------------------------------------------------------------------
## Hero · "the descent" (`#hero-3d`)
Landscape `f(x,y) = 0.5(x²−1)² + 0.6y² + 0.2x` on `x∈[−2.1,2.1], y∈[−1.7,1.7]` (two basins along x — the right one at x≈0.95 is shallower than the left one at x≈−1.05 — and a bowl along y), `fx = 2x(x²−1) + 0.2`, `fy = 1.2y`, zscale .75, as a lit `prim.surface` (res 80, ramp `[s1,s7,s2]`), a faint floor `prim.grid` below, the Unit 8 starfield in the dark theme. A glowing **walker** (`prim.dot`, s4, r≈.06, with a soft halo sprite via `sparkTex`) runs gradient descent with γ = 0.16 from a keyframe start; every step is tweened (0.42 s each, `Cinema.ease.out`), leaving a glowing **trail** (`ribbon`, s3, rebuilt every few steps or a `prim.path` extended per step) and, at the walker, two arrows: the **gradient** (s2, pointing uphill, length ∝ |∇f| capped) and the **step** (s3, `−γ∇f`). A `slot` label near the walker reads `step k · f = …`. When |∇f| < 0.03 or k = 34: hold 1.3 s with the walker pulsing, then fade the trail (0.7 s) and move to the next of 5 keyframe starts `[(1.9,1.3),(−0.3,−1.5),(1.6,−1.2),(−1.9,0.9),(0.35,1.55)]` (two of these end in the shallow right basin, three in the deep left one — the point of the picture). Camera `pos wide?[4.6,4.2,6.6]:[2.2,5.4,7.4]`, `look [0,0,.2]`, fov 32, `orbit:fine`, `autoRotate:.045`. Reduced motion: a still frame with the full path from start 1 drawn. Re-`remount` on the 760px width crossing like Unit 8.

## W1 · `#w-fit` — the fitting bowl (side SVG `#ft-svg` 320×260 + 3D stage `#ft-3d`)
The lecture's data: `(1,3.1),(2,4.9),(3,7.3),(4,9.1)`; model `ŷ = a x + b`; loss `L(a,b) = Σ (y_i − (a x_i + b))²`. Exact: `a* = 2.04, b* = 1, L* = 0.072` (verify by normal equations in code and print). Gradient `∂L/∂a = −2Σ x_i r_i`, `∂L/∂b = −2Σ r_i`, `r_i = y_i − (a x_i + b)`. Hessian `[[60,20],[20,8]]`, eigenvalues ≈ 66.80 and 1.20, `2/λmax ≈ 0.0299`.
Sliders `ft-a` (a ∈ [−1,5], step .01, default 0), `ft-b` (b ∈ [−4,6], step .01, default 0), `ft-g` (γ ∈ [0.002,0.034], step .001, default 0.02), `ft-k` (steps 1..60, default 20). Buttons: `ft-play` ▶ descend, `ft-reset` (a=b=0), presets `[data-p]`: `origin` (0,0), `far` (4,−3), `best` (2.04,1).
Side SVG: axes x∈[0,5], y∈[0,11]; the four points (s1 glowing dots), the current line (s4), the four residual segments (critical colour, thick) with a tiny `r = …` label each; title `L = …`.
3D stage: L(a,b) over `a∈[−1,5], b∈[−4,6]` as a lit `prim.surface` (res 72), ZS = 1.5/max L over the domain (compute), `liftedContours` at levels `[0.5,2,5,10,20,40,80,160]`, a glowing s4 dot ON the surface at the current (a,b) with a thin `tube` down to the floor, a small s3 dot at the minimum (2.04, 1), axis labels `a →`, `b →` via `prim.label`. ▶ descend: run `ft-k` GD steps from the current (a,b) with γ, tweening the dot along each step (0.12 s per step, chained via `tween`), drawing the path as a ribbon (s3) on the surface and updating the side SVG + sliders + outputs live (set `ft-a.value` etc.). Stop with verdict bad `✗ γ = … is above 2/λmax ≈ 0.030 — the steps grow instead of shrinking (diverging)` if |a| or |b| exceed 50. HUD: `L = … · ∇L = (…, …)`. Readout: `a = …, b = …`, `L(a,b) = …`, `∇L = (−2Σxᵢrᵢ, −2Σrᵢ) = (…, …)`, `best fit: a* = 2.04, b* = 1, L* = 0.072`, `stiff direction λ₁ ≈ 66.8 · flat direction λ₂ ≈ 1.20 · speed limit 2/λ₁ ≈ 0.030`. Verdict: good `✓ L is falling — the line is tilting toward the data` when the last step decreased L; info `at the bottom: ∇L ≈ 0` when |∇L| < 0.05; bad on divergence.

## W2 · `#w-flat` — the flat-spot prober, one dimension (SVG `#fp-svg` 600×340)
Tabs `fp-tabs`: `quartic` — the lecture's `l(x)=x⁴+7x³+5x²−17x+3` on `x∈[−5.6,1.7]` (stationary points **−4.480 (min, l=−47.07, l″=62.7), −1.432 (max, l=21.25, l″=−25.5), 0.662 (min, l=−3.84, l″=43.1)** — find them in code by scanning sign changes of l′ then bisection; do not hard-code); `cubic` — `f=2x³−9x²+12x+5` on `[−0.6,3.6]` (x=1 max f=10, x=2 min f=9); `bumpy` — `f=sin(3x)+0.3x²` on `[−3.2,3.2]` (several local minima).
Slider `fp-x` (x, range set per tab, step .01), `fp-g` (γ ∈ [0.002,0.2], default 0.02). Draw: the curve (s1, glowing), every stationary point as a dot coloured by type (min s3 · max s2) with a label `min` / `max`, the probe point (s4 glowing) with its tangent segment (s4) and a short uphill gradient arrow (s2) along the x-axis under it. ▶ `fp-play` "walk downhill": runs GD `x ← x − γ f′(x)` for up to 40 steps (stop when |f′| < 1e-3 or the iterate leaves the view), tweening the probe (0.16 s per step) and leaving faint dots; the slider/output follow. Readout: `f(x) = …`, `f′(x) = …` (`→ move left/right` hint from its sign), `f″(x) = …`, `stationary points: x = … (min, f = …), …` (list). Verdict: info `slope … : downhill is to the left/right`; when |f′| < 1e-3: good `✓ flat, and f″ > 0 — a valley (local minimum)` or bad `✗ flat, but f″ < 0 — a hilltop: one nudge and descent leaves` ; for the bumpy tab after a walk: info `landed in the nearest valley — gradient descent finds a local minimum, not the global one`.

## W3 · `#w-desc` — the descent stage (3D `#ds-3d`)
Quadratics `f(x) = ½ xᵀAx + bᵀx`, `∇f = Ax + b`, minimiser `x* = −A⁻¹b`. Presets `ds-presets [data-p]`:
- `lecture` (default): `A=[[2,1],[1,20]], b=(−5,−3), x₀=(−3,−1)`; `x* = (97/39, 1/39) ≈ (2.487, 0.026)`, `f* ≈ −6.256`; eigenvalues `11 ± √82 ≈ 20.055, 1.945`; domain x∈[−4,5.5], y∈[−2.6,2.2].
- `round`: `A=[[2,0],[0,2]], b=(−5,−3), x₀=(−3,−1)`; x*=(2.5,1.5); domain x∈[−4,5.5], y∈[−2.6,4].
- `stretch`: `A=[[2,0],[0,6]], b=(0,0), x₀=(2,2)` (= `x²+3y²`); domain [−2.6,2.6]².
Compute eigenvalues in code from A (symmetric 2×2). Tabs `ds-tabs`: `fixed` (γ from slider `ds-g`, range [0.005,0.13] step .005, default 0.05) | `exact` (line search `γ* = gᵀg / gᵀAg` each step; slider disabled, output `auto`). Slider `ds-k` steps (1..40, default 20). Buttons `ds-play` ▶ run, `ds-reset`.
Stage: floor grid, lit surface (res 72, ramp [s1,s7,s2]), ZS = 1.6/(zmax−zmin) over the domain, `liftedContours` at 10 levels between f* and zmax, glowing s3 dot at x* with a vertical `tube`, the walker (s4 dot + halo) at the current iterate ON the surface, its gradient arrow (s2, uphill, scaled ≤ 1 unit) and its step arrow (s3). Path: ribbon (s3) through all iterates + small dots. ▶ run: `ds-k` steps tweened 0.14 s each; on `fixed` stop early with a bad verdict when |x| > 40 (`✗ diverging: γ = … > 2/λ₁ = …`). `ds-reset` clears the path and returns to x₀. Preset change → `remount(S)` (approved pattern) + reset. HUD: `k = … · f = … · |∇f| = …`. Readout: `xₖ = (…, …)`, `∇f = Axₖ + b = (…, …)`, `γ used = …` (for `exact`: `γ* = gᵀg/gᵀAg = …/… = …`), `f(xₖ) = …`, `x* = −A⁻¹b = (…, …), f* = …`, `λ₁ = …, λ₂ = … · factor per step: |1 − γλ₁| = …, |1 − γλ₂| = …` (fixed tab). Verdict (fixed): bad `✗ γ > 2/λ₁ = … — diverging`; info `γ close to 2/λ₁: the stiff direction overshoots and bounces (zig-zag)` when 1−γλ₁ < −0.3; info `γλ₂ = … is tiny: the flat direction crawls` when 1−γλ₂ > 0.95 and not overshooting; good `✓ every direction shrinks: |1−γλ| < 1 for both eigenvalues` otherwise. Verdict (exact): good `✓ each step turns exactly 90° — the new gradient is perpendicular to the last direction` (compute the angle between successive step directions and print it: `turn = …°`).

## W4 · `#w-step` — the step-size dial (SVG `#st-svg` 600×340)
Tabs `st-tabs`: `one` (default) | `two`.
`one`: `f = ½λx²`, sliders `st-l` (λ ∈ [0.5,4], step .1, default 2), `st-g` (γ ∈ [0.02,1.3], step .01, default 0.3), start x₀ = 1.8, 12 steps. Draw the parabola (s1) on x∈[−2.4,2.4] (y autoscaled to f(2.4)); the iterates as glowing dots on the curve joined by straight "bounce" segments (s3 when |x| shrinks, critical colour when it grows), numbered 0,1,2… near the first six; a HUD-style corner text `factor 1 − γλ = …`. Presets `st-presets [data-p]`: `crawl` (γλ=0.1), `sweet` (γλ=1 → one step lands exactly at 0), `bounce` (γλ=1.6), `edge` (γλ=2), `blow` (γλ=2.4) — set γ = value/λ (clamp into the slider range and refresh). ▶ `st-play`: the dots appear one by one (0.22 s each). Readout: `xₖ₊₁ = xₖ − γ·λxₖ = (1 − γλ)·xₖ`, `1 − γλ = …`, `|x₁₂| / |x₀| = …` (or `(1−γλ)¹² = …`), `steps to shrink |x| by 100× ≈ ⌈ln 100 / |ln|1−γλ||⌉ = …` (print `never` when |1−γλ| ≥ 1). Verdict: good `✓ |1−γλ| < 1 and positive: smooth descent` ; good `✓ |1−γλ| < 1 but negative: overshoots the bottom each step, still shrinking`; info `γλ = 2: bounces between ±x₀ forever`; bad `✗ |1−γλ| > 1: every step is bigger than the last — diverging`; special `γλ = 1 → x₁ = 0: one perfect step` (good).
`two`: `f = ½(λ₁x² + λ₂y²)`, sliders `st-l1` (λ₁ ∈ [1,20], default 20), `st-l2` (λ₂ ∈ [0.5,5], default 2), `st-g` shared (γ range for this tab [0.005,0.15]; when switching tabs set the slider's min/max/value and refresh its output), start (1.8,1.8), 16 steps. Draw the ellipse contours (`contourSegs` at 8 levels, s1 faded) on a `plane(svg,-2.2,2.2,-2.2,2.2,…)` and the path (glowing s3 polyline + dots, critical-coloured segments when f increases). Readout: `factor along x: 1 − γλ₁ = …`, `factor along y: 1 − γλ₂ = …`, `speed limit 2/λ₁ = …`, `condition number λ₁/λ₂ = …`. Verdict: bad when γ > 2/λ₁ (`✗ stiff direction diverges`), info `zig-zag: the stiff direction bounces (factor …) while the flat one crawls (factor …)` when 1−γλ₁ < 0 and 1−γλ₂ > 0.7, good `✓ both factors inside (−1, 1): converging` otherwise. Presets for this tab: `safe` (γ = 0.045), `zigzag` (γ = 0.095), `over` (γ = 0.12) — show/hide the two preset rows per tab.

## W5 · `#w-line` — the line-search lens (SVG `#ls-svg` 600×340 + side SVG `#ls-h` 300×160)
Tabs `ls-tabs`: `stretch` (default): `f = x² + 3y²`, start (2,2) | `lecture`: `A=[[2,1],[1,20]], b=(−5,−3)`, start (−3,−1) (draw on x∈[−4,4.5], y∈[−2,2.4] for lecture; [−2.6,2.6]² for stretch). Main SVG: contours (`contourSegs`, 9 levels, s1 faded, on a `plane`), the current point `xₖ` (s4 glowing), the descent **ray** `xₖ − γ g` drawn across the plot as a dashed s3 line, a **bead** on the ray at the slider's γ (`ls-g`, range [0, γmax] where γmax = 2.2·γ*, step γmax/200, default = 0.5·γ*; when the point or tab changes, set min/max/value and refresh the output), the minimiser `x*` (s3 dot). At the bead, the gradient of f (s2 arrow, scaled ≤ 1.2 units) — when the bead sits at γ*, this arrow is perpendicular to the ray and the ray is tangent to the contour through the bead (draw that contour brighter). Side SVG: `h(γ) = f(xₖ − γ g)` on [0, γmax] (s7 curve), the bead's height (s4 dot), γ* marked with a dashed drop-line and label `γ* = …`. Buttons: `ls-best` "snap to γ*" (tween the bead to γ*), `ls-step` "take the step" (commit: xₖ ← xₖ − γ* g, redraw ray, keep the old segments as a glowing path with the 90° corners marked by a tiny square), `ls-reset`. Readout: `g = ∇f(xₖ) = (…, …)`, `h(γ) = f(xₖ − γ g)`; for `stretch` also `= (1−2γ)²x² + 3(1−6γ)²y²` and `γ* = (x²+9y²)/(2x²+54y²) = …/… = …`; for both `γ* = gᵀg / gᵀAg = … / … = …`; `new point = (…, …)`, `f: … → …`, `angle(old direction, new gradient) = …°`. Verify in code: for `stretch` at (2,2): γ* = 5/28 ≈ 0.1786, new point (9/7, −1/7) ≈ (1.286, −0.143), f 16 → 12/7 ≈ 1.714; next γ* = 5/12, point (3/14, 3/14). Verdict: good `✓ at γ* the ray just kisses a contour — the new gradient is perpendicular to the ray (…°)` when the bead is at γ* (within 1%); info `not there yet: h′(γ) = −gᵀ∇f(bead) = … ≠ 0 — sliding further down the ray still helps` otherwise (print the sign meaning: negative → go further, positive → went too far).

## W6 · `#w-bracket` — the bracket squeezer (SVG `#br-svg` 600×340)
Function tabs `br-fn`: `practice` (default): `h(α) = α² − 5α + 8` on `[0,4]` (minimiser 2.5) | `bumpy`: `h(α) = 0.4(α−1.2)² + 0.35 sin(4.2α) + 1.5` on `[0,4]` (NOT unimodal — several local dips; find its global minimiser in code by a fine scan and print it). Method tabs `br-m`: `binary` | `golden` | `armijo`.
Draw h (s1), the current bracket `[a,b]` as a translucent s4 band, the evaluated points as s7 dots (with tiny labels `m`, `m+ε` / `m₁`, `m₂`), the true minimiser (s3 dot), and for armijo the **Armijo line** `h(0) + c·α·h′(0)` with c = 0.1 (dashed s2) and the candidate α as a bead.
- binary: midpoint `m=(a+b)/2`; compare `h(m+ε)` with `h(m)` (ε=1e-6): increasing → `[a, m]`; decreasing → `[m, b]`. (For the practice function: [0,4]→[2,4]→[2,3]→[2,2.5]; assert this in code and print.)
- golden: `m₁ = b − φ(b−a)`, `m₂ = a + φ(b−a)` with `φ = (√5−1)/2 ≈ 0.618`; if `h(m₁) < h(m₂)` → `[a, m₂]` (and the old m₁ becomes the new m₂ — reuse, count evaluations) else `[m₁, b]`.
- armijo (backtracking from αmax = 4, halving): accept α if `h(α) ≤ h(0) + c·α·h′(0)`; else α ← α/2. Show each rejected α as a critical-coloured dot on the line test and the accepted one glowing s3.
Buttons `br-step` (one iteration), `br-play` ▶ (auto-run 8 iterations, 0.35 s each), `br-reset`. Readout: `bracket [a, b] = [… , …]`, `width = … (after k iterations: (b−a)/2ᵏ = … for binary, (b−a)·0.618ᵏ for golden)`, `evaluations of h so far = …`, `true minimiser α* = …`; for armijo: `α tried: … → accepted α = … (h = … ≤ …)`. Verdict: good `✓ the bracket still contains α*` / bad `✗ the bracket lost α* — h is not unimodal here, so exact bracketing can lie` (bumpy) / for armijo: good `✓ sufficient decrease reached after … halvings — inexact, cheap, and no unimodality needed`. Also print `iterations for width ≤ 0.01 from [0,4]: binary 9 · golden 13` (compute with logs, print the integers).

## W7 · `#w-decay` — schedules and the bold driver (SVG `#dc-svg` 600×320)
Tabs `dc-tabs`: `schedules` (default) | `bold`.
`schedules`: sliders `dc-a0` (α₀ ∈ [0.1,1.5], default 0.8), `dc-k` (k ∈ [0.01,0.3], default 0.05), `dc-T` (epochs shown, 10..60, default 30). Chart of αₜ vs t (t = 0..T) for exponential `α₀e^{−kt}` (s1), inverse `α₀/(1+kt)` (s7), step decay `α₀/3^{⌊t/5⌋}` (s2, a staircase); a glowing marker on each at `dc-t` (slider t ∈ [0,T], integer, default 10); y-axis from 0 to α₀·1.1. Readout: `t = …: exponential …, inverse …, step …`, `inverse decay reaches α₀/2 at t = 1/k = …`, `exponential reaches α₀/e at t = 1/k = …`. (Practice numbers: α₀=0.8, k=0.05: t=10 → 0.4852 / 0.5333 / 0.0889 (step, epoch 12 → 0.8/9); t=20 → 0.2943 / 0.4.) Verdict info `exponential falls fastest (geometric), inverse slowest (harmonic), step falls in cliffs`.
`bold`: bold-driver GD on `f = ½(λ₁x² + λ₂y²)` with λ₁=20, λ₂=2 from (1.8,1.8), α₀ = 0.02: each step: try `x − αₜ∇f`; if f improved → keep it and `α ← 1.05α`; else undo and `α ← 0.5α`. Run 40 steps. Chart: αₜ (s7 line with dots; undone steps marked with a critical-coloured × ) and f (s1, log scale on a second axis or normalised — pick log10 f mapped to the same height and label the axis) vs t. Readout: `step t = …: α = …, f = …`, `increases: …, halvings: …`, `final α ≈ …` (it should settle near 2/λ₁ = 0.1 region then oscillate — print `settles near the speed limit 2/λ₁ = 0.1`). ▶ `dc-play` draws the run point by point (0.08 s each). Verdict good `✓ the driver speeds up while it can and halves the moment it overshoots — monotone descent guaranteed`.

## W8 · `#w-fd` — the gradient auditor (SVG `#fd-svg` 600×340)
`J(w₁,w₂) = w₁³ + w₁w₂²` at `(2,1)`: analytic `∂J/∂w₁ = 3w₁² + w₂² = 13`, `∂J/∂w₂ = 2w₁w₂ = 4`. Tabs `fd-tabs`: `w1` (default) | `w2`. Slider `fd-d` = log₁₀Δ ∈ [−9, 0] (step 0.05, default −1 → Δ = 0.1). Chart (log-log): x = Δ from 1e−9 to 1, y = |error| from 1e−12 to 10; two curves computed in JS float64 for 90 sample Δ's: forward `(J(w+Δeᵢ) − J(w))/Δ` (s2) and central `(J(w+Δeᵢ) − J(w−Δeᵢ))/(2Δ)` (s3), both glowing; reference slopes drawn dashed (`∝ Δ` and `∝ Δ²`) and labelled; the bead at the chosen Δ on each curve with labels; the round-off region (Δ < 1e−5) shaded faintly with a label `round-off takes over`. Readout: `Δ = …`, `J(w) = 10`, `forward: (J(w+Δ) − J(w))/Δ = … (error …)`, `central: … (error …)`, `analytic = 13` (or 4), `error ratio forward/central = …`. Practice numbers to reproduce at Δ=0.1: forward 13.61 (error 0.61), central 13.01 (error 0.01); for w₂: forward 4.20 (error 0.20), central exactly 4 (the function is quadratic in w₂ — say so: `central is exact here`). Verdict: good `✓ central error ≈ Δ² · |J‴|/6 — halve Δ and it drops 4×; forward only 2×`; info `Δ so small that floating-point round-off dominates — the estimate gets worse, not better` when Δ < 3e−6; bad `✗ Δ = 1: the secant is nowhere near the tangent` when Δ ≥ 0.5.

## W9 · `#w-sgd` — the noisy descent (SVG `#sg-svg` 600×340 + side SVG `#sg-loss` 300×150)
Data: N = 40 points `xᵢ = −2 + 4(i−1)/39`, `yᵢ = 1.5xᵢ + 0.5 + nᵢ` where nᵢ is deterministic noise in [−0.7,0.7] from an LCG (`s = (s·1664525 + 1013904223) mod 2³²`, seed 7, `n = (s/2³² − 0.5)·1.4`). Model `ŷ = a x + b`, loss `L(a,b) = (1/N) Σ (yᵢ − a xᵢ − b)²`, full gradient `(−(2/N)Σxᵢrᵢ, −(2/N)Σrᵢ)`; minibatch gradient uses `|S|` indices drawn (deterministic LCG, separate stream) with the same formula over S. Compute `(a*,b*)` by the normal equations in code and print. Sliders: `sg-batch` (batch size ∈ {1,2,4,8,16,40} — use a range 0..5 mapped to that list, output shows the size), `sg-g` (γ ∈ [0.01,0.3], default 0.1), `sg-T` (steps 10..200, default 80). Tabs `sg-tabs`: `constant` γ | `decay` `γₜ = γ/(1 + 0.05t)`. Main SVG: contours of L over `a∈[−0.5,3.5], b∈[−1.5,2.5]` (`contourSegs`, 9 levels, s1 faded) on a `plane`, the minimiser (s3 dot), the path from (0,0) (`sg-start` presets: `origin` (0,0), `far` (3.2,−1.2)) as a glowing polyline (s4) with faint dots; at the current point the **true** gradient arrow (s2) and the **minibatch** gradient arrow (s7) so the noise is visible. Side SVG: `L(aₜ,bₜ)` vs t (s4) with the full-batch loss curve for the same γ overlaid (s3, thin) for comparison; log-ish y (use log10 mapped). ▶ `sg-play` draws the run (0.03 s per step), `sg-reset`. Readout: `batch |S| = … of 40 → cost per step = … residuals`, `true ∇L = (…, …), minibatch estimate = (…, …), angle between them = …°`, `L after … steps = … (full batch would give …)`, `best: a* = …, b* = …, L* = …`. Verdict: info `|S| = 1: each step is 40× cheaper, the direction is noisy, the walk jitters around the bottom` (batch 1–2 constant), good `✓ decaying γ: the jitter shrinks as t grows — the average of many small noisy steps is the true step` (decay tab), good `✓ |S| = 40: the exact gradient — smooth, but every step costs all the data` (batch 40), info otherwise `bigger batch → less variance (∝ 1/|S|) → smoother, dearer steps`. Also print `variance of the estimate ∝ 1/|S|: relative to |S| = 1 this batch is …× less noisy`.

---------------------------------------------------------------------------
## Deliverable + self-check
1. Write `tpl/u9-widgets.html` (nine widgets with markers) and `tpl/u9-widgets.js` (nine widgets + the hero, in that order; ~1000–1400 lines expected).
2. Build a throwaway test page: copy `src/unit-08.html`, replace everything between `<main id="main-content" tabindex="-1">` and `</main>` with the nine widget blocks (plain concatenation, drop the markers), keep the hero markup (`#hero-3d`) but shorten its aria-label, replace the script body between `/* ---------- shared helpers (house runtime) ---------- */` and the `})();` that closes the main IIFE with `tpl/u9-shared.js` + `tpl/u9-widgets.js`, save as `src/_u9test.html`, then `cd /home/claude/mfml-site/dev && node build.js _u9test.html`.
3. Run it with WebGL Playwright (launch args `--use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader`, `executablePath: process.env.PW_CHROMIUM` = `/opt/pw-browsers/chromium`; the built page is `site/_u9test.html`; for `file://` the page needs `vendor/three.min.js` next to it — `site/vendor` exists): assert zero console errors / page errors; click every `.widget button`, drive every range to min and max (dispatch `input`), press every ▶ and wait for it; screenshot each widget at 1440 and 390 wide into `shots-cinema/u9test/` and LOOK at every screenshot with the Read tool; fix what looks wrong (labels colliding, blank stages, unreadable text, empty plots, paths leaving the viewBox). Also flip to `data-theme="light"` and screenshot the hero + W1 + W3.
4. Verify these numbers print: W1 best fit `a* = 2.04, b* = 1, L* = 0.072`; W2 quartic stationary points −4.480 / −1.432 / 0.662; W3 lecture `x* ≈ (2.487, 0.026)`, `f* ≈ −6.256`, λ ≈ 20.055 / 1.945, and γ = 0.1 diverges while γ = 0.05 converges; W5 stretch γ* = 0.1786, new point (1.286, −0.143), f 16 → 1.714, angle 90.0°; W6 binary trace [0,4]→[2,4]→[2,3]→[2,2.5]; W7 t=10 → 0.4852 / 0.5333; W8 Δ=0.1 → 13.61 / 13.01.
5. Delete `src/_u9test.html` and `site/_u9test.html` when done. Report (≤300 words): what each widget does, any deviation from this spec and why, the exact readout strings a verify script can assert on, and anything you could not do.
