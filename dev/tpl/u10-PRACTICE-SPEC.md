# Unit 10 · Optimization I — Gradients that Work: practice arena spec

You are writing TWO files:
1. `/home/claude/mfml-site/dev/tpl/u10-practice.html` — the practice-arena section body (twelve `.prob` cards, nothing else: no `<section>` wrapper, no heading, no intro prose, no next-card).
2. `/home/claude/mfml-site/dev/verify-math10.py` — a sympy/numpy script in the exact style of `verify-math9.py` (read it) that machine-checks EVERY number printed in your twelve solutions plus the "also verify" list below, printing `ok` lines and ending with a count. It must run clean with `cd /home/claude/mfml-site/dev && python3 verify-math10.py` (sympy and numpy are installed).

## Read first (house style — copy it exactly)
- `tpl/u9-practice.html` (all twelve cards): markup, `.pstep`, `.pans`, `.callout remember` inside the solution, the `<b>(a)</b>` part labels, display maths broken with `aligned`/`\\` so nothing overflows a 44rem column (probe: no single display line wider than ~450 px at 390 px viewport — break long lines), `\(…\)`/`\[…\]` raw KaTeX (KaTeX 0.18.4: no `\xrightarrow`, no wide `\text`, keep `∎`/✓ outside math or use `\blacksquare`), tables of iterations written as `<table class="plain">` inside the step (one computation per line, a row per iteration: k · point · gradient · new point · f).
- Sources: `content/practice10.txt` (seven fully worked problems) and `content/companion10.txt` (worked examples and the "check yourself" answers). Your job is to re-set them in the house format, keep every number, make every algebraic step visible (one computation per line), and write the "what this tests / plan" line and the "remember" takeaway for each. **Fix the sources' own slips silently** — known ones: (i) the companion's gradient-descent table for the three-point example prints J = 15.19 and 2.99 at steps 1 and 2; the correct values are **15.27 and 3.13** (J = 14w² − 66w + 78 at w = 1.320 and 1.901); (ii) the lecture's memoriser solution `w₂ = 7` is wrong, the exact solution is `w₂ = 12` (Problem 8 makes a point of it). Present it as a plain topic-wise practice set: never mention where the problems come from, never mention exams, question papers, marks, quizzes, rubrics or "papers ask"; the difficulty tag replaces the marks. Never write MFML/ZC416/BITS/WILP.
- Refer to the unit's ideas by role, not by section number: "the bill for being wrong", "the parachute drop", "knobs versus facts", "the hair-trigger knob", "the three repairs (centre, standardise, min–max)", "the watershed", "the combination lock", "the salt flat", "the canyon", "the per-direction factor 1 − ηλ", "the speed limit 2/λ_max", "the condition number κ", "change the units, not the model".

## Card format
```
  <!-- ===== Problem N ===== -->
  <div class="prob reveal">
    <div class="prob-head"><span class="ptag">Problem N</span><span class="pdiff easy|medium|hard">easy|medium|hard</span></div>
    <p class="pq">… the question, parts as <b>(a)</b> <b>(b)</b> …</p>
    <p class="ptest"><b>What this tests.</b> one sentence. <b>Plan.</b> one or two sentences on the order of moves, what to compute first, the check at the end.</p>
    <details class="sol"><summary>Show the full solution</summary>
      <div class="pstep"><b>(a) Step 1 — name of the move.</b> … every algebraic step; one computation per line inside \[\begin{aligned}…\end{aligned}\] when there are several …</div>
      …
      <p class="pans">answers at a glance: (a) … (b) … (c) …</p>
      <div class="callout remember reveal"><span class="tag">Remember</span><p>the one idea to keep, and where this type usually goes wrong.</p></div>
    </details>
  </div>
```
Every part must have a definite computable answer (numbers, vectors, matrices, counts, ranges) — no "explain" or "discuss" parts; where a source part asks to "comment" or "name the technique", turn it into a computable statement (e.g. "state the range of η for convergence and which coordinate sets it" / "give the transformed value and say whether it lies in [0,1]").

## The twelve problems (key numbers to reproduce exactly)
1. (easy) Column {30, 50, 70, 90, 110}: (a) min–max → 0, 0.25, 0.5, 0.75, 1; (b) standardise (population σ): μ = 70, deviations (−40, −20, 0, 20, 40), σ² = 800, σ ≈ 28.28, z = (−1.414, −0.707, 0, 0.707, 1.414), check Σz = 0 and Σz² = 5; (c) unseen x = 130 with the training statistics: min–max 1.25 (outside [0,1]), z = 2.12.
2. (easy) Partials ∂J/∂w₁ = −640, ∂J/∂w₂ = −1.6, γ = 10⁻³: (a) Δw₁ = 0.64, Δw₂ = 0.0016 (ratio 400 : 1); (b) steps for w₂ to move 1 unit ≈ 625, during which w₁ moves 400 units; (c) γ so that Δw₁ = 0.01 → γ = 1.5625 × 10⁻⁵, then Δw₂ = 2.5 × 10⁻⁵ and ≈ 40 000 steps; (d) the fix, stated as a computable claim: after standardising both columns the two partials have the same order of magnitude, so one γ serves both (no number needed, one line).
3. (easy) Three receipts x = (1, 2, 3), y = (2, 5, 7), model ŷ = wx: (a) J(w) = (w−2)² + (2w−5)² + (3w−7)² = 14w² − 66w + 78; (b) dJ/dw = 28w − 66 → w* = 33/14 ≈ 2.357, d²J/dw² = 28 > 0, J* = 3/14 ≈ 0.214 (show 14·(33/14)² − 66·33/14 + 78 = 1089/14 − 2178/14 + 1092/14 = 3/14); (c) GD from w₀ = 0, η = 0.02, five steps in a table: w = 0, 1.320, 1.901, 2.156, 2.269, 2.318; slope = −66, −29.04, −12.78, −5.62, −2.47, −1.09; J = 78, 15.27, 3.13, 0.78, 0.32, 0.24; (d) the per-step factor 1 − 28η = 0.44 and the largest convergent η = 2/28 = 1/14 ≈ 0.0714.
4. (easy) The lecture's two bowls: (a) L = x² + y²: updates x ← (1−2η)x, y ← (1−2η)y; η = 0.5 sends any point to (0,0) in one step; (b) L = x² + 4y²: factors (1−2η), (1−8η); stability |1−8η| < 1 ⇒ η < 0.25, set by y (∂²L/∂y² = 8); (c) trace η = 0.24 from (10,10) for three steps: (5.2, −9.2), (2.704, 8.464), (1.406, −7.787); L = 500 → 365.6 → 293.9 → 244.5; (d) Hessian diag(2, 8), κ = 4; the y-coordinate flips sign each step (factor −0.92) while x shrinks by 0.52 per step.
5. (medium) House prices x₁ = (1000, 1500, 2000), x₂ = (2, 3, 3), y = (50, 75, 90), J = Σ(w₁x₁ + w₂x₂ − y)²: (a) at w = 0, ∂J/∂w₁ = −2·342 500 = −685 000, ∂J/∂w₂ = −2·595 = −1190, ratio ≈ 576; (b) standardise: μ₁ = 1500, σ₁ ≈ 408.25, z₁ = (−1.2247, 0, 1.2247); μ₂ = 8/3 ≈ 2.667, σ₂ ≈ 0.4714, z₂ = (−1.4142, 0.7071, 0.7071); partials −97.98 and −91.92; (c) new ratio ≈ 1.07.
6. (medium) L = 9x² + y²: (a) factors (1−18η), (1−2η); convergence 0 < η < 1/9 ≈ 0.111 (set by x); (b) η = 0.1 from (1, 3): (−0.8, 2.4) → (0.64, 1.92) → (−0.512, 1.536); x oscillates (factor −0.8 < 0 because η > 1/18 ≈ 0.056), y descends monotonically (factor 0.8); (c) H = diag(18, 2), κ = 9; equal curvature (κ = 1, e.g. 9x² + 9y²) is fastest and η = 1/18 then finishes in one step; with κ = 9 no single η finishes in one step (would need 1/18 and 1/2 at once).
7. (medium) Least squares J = Σ(wᵀXᵢ − yᵢ)²: (a) derive ∇J = 2Σ(wᵀXᵢ − yᵢ)Xᵢ by the chain rule (rᵢ = wᵀXᵢ − yᵢ, ∇rᵢ = Xᵢ); (b) X₁ = (2,1), y₁ = 4, X₂ = (1,3), y₂ = 5, w₀ = 0: r = (−4, −5), J₀ = 41, ∇J = 2[(−4)(2,1) + (−5)(1,3)] = (−26, −38); w₁ = (0.52, 0.76) with γ = 0.02; new residuals (−2.20, −2.20), J₁ = 9.68 < 41 (76 % of the loss removed in one step).
8. (medium) Knobs versus facts: rows (x₁,x₂,x₃,x₄,y) = (61,2,3,0.1,59), (40,0,4,0.5,40), (68,0,10,1.0,70), model ŷ = w₁x₁ + w₂x₂ + w₃x₃ + w₄x₄ + w₅: (a) the honest model w = (1,0,0,0,0): ŷ = (61, 40, 68), errors (2, 0, −2), J = 8; (b) the memoriser with w₁ = w₄ = 0 must solve 2w₂ + 3w₃ + w₅ = 59, 4w₃ + w₅ = 40, 10w₃ + w₅ = 70 → w₃ = 5, w₅ = 20, w₂ = 12, J = 0; (c) check the candidate (0, 7, 5, 0, 20): row 1 gives 14 + 15 + 20 = 49 ≠ 59, so its J = 100, not 0; (d) an unseen row x = (50, 1, 6, 0.3) with true y ≈ 50: honest ŷ = 50 (error 0), memoriser ŷ = 12 + 30 + 20 = 62 (error 12); (e) count: 5 unknowns, 3 equations → the system 3 equations in 5 unknowns leaves 2 free parameters, i.e. infinitely many zero-error solutions.
9. (medium) F(x) = (x−1)²((x−3)² − 1): (a) F′(x) = 2(x−1)(2x² − 10x + 11) = 4x³ − 24x² + 42x − 22; stationary points x = 1, (5−√3)/2 ≈ 1.634, (5+√3)/2 ≈ 3.366; (b) F″(x) = 12x² − 48x + 42: F″(1) = 6 > 0 (min), F″(1.634) ≈ −4.39 (max), F″(3.366) ≈ 16.39 (min); F(1) = 0, F(1.634) ≈ 0.348, F(3.366) ≈ −4.848; (c) GD from x₀ = 0.5, η = 0.02, four steps: x = 0.5000, 0.6300, 0.7113, 0.7679, 0.8097 with F′ = −6.500, −4.065, −2.829, −2.089 and F = 1.3125, 0.6321, 0.3532, 0.2146, 0.1376; limit x = 1, F = 0 (the local valley) — any start below 1.634 ends there; (d) plateau arithmetic: |F′| = 0.001, η = 0.01 → step 10⁻⁵; width 2 → 200 000 steps.
10. (hard) F(x) = 3x⁴ − 4x³ − 12x² + 5, basins: minima at x = −1 (local) and x = 2 (global), watershed at x = 0 (verify: F′ = 12x(x−2)(x+1); F(−1) = 0, F(2) = −27, F(0) = 5); x₀ uniform on [−2, 3]: (a) P(global) = 3/5 = 0.6; (b) restarts: 1 − 0.4ᵏ ≥ 0.99 → 0.4⁵ = 0.01024 (fails), 0.4⁶ = 0.004096 (works) → k = 6, success 0.9959; (c) ten independent coordinates with p = 0.6: 0.6¹⁰ ≈ 0.006.
11. (hard) Guns and butter x₁ = (0.1, 0.8, 0.4), x₂ = (25, 10, 10), y = (7, 1, 4), ŷ = w₁x₁ + w₂x₂: (a) J(w) written out; H = 2·[[Σx₁², Σx₁x₂],[Σx₁x₂, Σx₂²]] with the sums 0.81, 14.5, 825 → H = [[1.62, 29],[29, 1650]]; eigenvalues ≈ 1650.5 and 1.11 (exact: 825.81 ± √(824.19² + 29²)), κ ≈ 1487, speed limit 2/λ_max ≈ 0.00121; (b) standardise: μ₁ = 0.4333, σ₁ = 0.2867, z₁ = (−1.163, 1.279, −0.116); μ₂ = 15, σ₂ = 7.071, z₂ = (1.414, −0.707, −0.707); sums Σz₁² = 3, Σz₂² = 3, Σz₁z₂ = −2.466; H_std = [[6, −4.93],[−4.93, 6]]; eigenvalues 6 ± 4.93 = 10.93, 1.07; κ ≈ 10.2; 2/λ_max ≈ 0.183; (c) improvement in κ ≈ 145×, in the admissible stride ≈ 151×; the leftover κ = 10.2 (not 1) comes from the off-diagonal −4.93, i.e. the two columns are correlated (correlation coefficient = −2.466/3 ≈ −0.822).
12. (hard) x₁ = (100, 500, 900), x₂ = (0.9, 0.3, 0.6), H = 2ΣXᵢXᵢᵀ: (a) sums 1 070 000, 780, 1.26 → H = [[2.14 × 10⁶, 1560],[1560, 2.52]]; λ_max ≈ 2.14 × 10⁶, λ_min ≈ 1.38; γ_max = 2/λ_max ≈ 9.3 × 10⁻⁷; κ ≈ 1.6 × 10⁶; (b) min–max: x₁′ = (0, 0.5, 1), x₂′ = (1, 0, 0.5); sums 1.25, 0.5, 1.25 → H′ = [[2.5, 1],[1, 2.5]]; eigenvalues a ± b = 3.5, 1.5; γ′_max = 2/3.5 ≈ 0.571; κ′ ≈ 2.33; (c) improvement factor ≈ 6 × 10⁵.

## Also verify in verify-math10.py (numbers the sections and widgets lean on)
- Hero/W10 blend: for t ∈ {0, 0.5, 1}, H(t) = 2 Z(t)ᵀ Z(t) with z_j(t) = (x_j − t μ_j)/σ_j^t on the guns-and-butter columns; at t = 0 eigenvalues 1650.51 / 1.11, at t = 1 10.93 / 1.07; min–max at t = 1: H = 2·diag(1.1837, 1) → κ ≈ 1.18; PCA-whitened: κ = 1 exactly (build it: standardise, eigendecompose the 2×2 covariance Zᵀ Z / 3, rotate and scale).
- W1: J(w) = 14w² − 66w + 78 trace at η = 0.02 (six w's, six J's above) and the divergence threshold η = 1/14.
- W2: with x = 200, η = 10⁻³ the factor 1 − 2ηx² = −79; the safe zone η < 1/x² = 2.5 × 10⁻⁵; from w₀ = 1 the iterate exceeds 1.8 × 10³⁰⁸ (overflows in float64) within 165 steps — compute the step count where |w| first exceeds 1e308 in exact arithmetic (79ᵏ·|w₀ − y/x|) and print it.
- W3: memoriser predictions on the unseen row for x₁ = 40, 50, 60 all equal 62.
- W4: x₁ = 50, x₂ = 4: x₁² = 2500, x₂² = 16, ratio 156.25.
- W6: the lecture F: watershed 1.634; P(global | x₀ ~ U[0,4]) = (4 − 1.634)/4 ≈ 0.5915; k for 0.99 at p = 0.5915: ⌈ln 0.01 / ln 0.4085⌉ = 6.
- W7 salt flat: the analytic gradient of f(x,y) = 1.6·σ(−3(x+1.4)) + 0.02x + 0.35y² − 1.2·exp(−((x−2.2)² + y²)/0.9) matches central differences at three points to 1e-6; |∇f| < 0.03 somewhere on the shelf x ∈ [−0.6, 0.8], y = 0.
- W8 canyon: the c = 4, η = 0.24 trace from (10,10) (six points and seven L values above); the one-step finish for c = 1 at η = 0.5; the stability bound η < 1/c.
- W9 compass: on F = x² + 4y² at (2.2, 1.1): ∇F = (4.4, 8.8); the angle between ∇F and the direction to the origin (−2.2, −1.1) measured as the angle between ∇F and (2.2, 1.1): compute and print (≈ 36.87°); the angle between ∇F and the contour tangent (−8.8, 4.4) is exactly 90°; on the circle the angle to the centre is 0.
- Contour orthogonality in general: for F(x + δ) ≈ F(x) + ∇F·δ, check numerically on the ellipse that moving 1e-4 along (−F_y, F_x) changes F by O(1e-8).
- High-dimensional count: for a separable F = ΣAᵢ(xᵢ) with kᵢ local minima each, the count of local minima is Πkᵢ — check by brute force for A(x) = F (the lecture quartic-like function with 2 minima) in d = 3 → 8 minima found by descent from a grid of starts.
- Companion Q2: L = x² + 9y²: η < 1/9; Q3: z = (56 − 40)/8 = 2.

## Deliver
Write both files. Run `python3 verify-math10.py` until every check passes. Then report (≤250 words): the twelve problems with difficulty tags, each "remember" takeaway in one line, and the list of asserted numbers.
